# AlpacaHTTP Configuration

# HTTP server settings
http:
  port: 6800
  thread_pool_size: 32  # Number of concurrent request handlers (increase for more devices/clients). Env: ALPACAHTTP_THREAD_POOL_SIZE (1-256)

# Discovery settings
discovery:
  enabled: true

# Logging settings
logging:
  level: WARNING  # DEBUG, INFO, WARNING, ERROR
  history_limit: 2000  # 0 = unlimited, Env: ALPACAHTTP_LOG_HISTORY_LIMIT

# Server information
server:
  name: AlpacaHTTP
  manufacturer: OpenAstro.net
  location: ""

# Device enable/disable (optional)
# devices:
#   camera:
#     0: true
#     1: false
#   mount:
#     0: true